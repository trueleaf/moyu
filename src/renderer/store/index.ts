import { createStore } from "vuex"
import permission from "./permission/permission";

export default createStore({
    state: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        permission,
    }
})
